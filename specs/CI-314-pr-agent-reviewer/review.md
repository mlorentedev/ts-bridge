---
spec: "CI-314-pr-agent-reviewer"
verdict: "FAIL"
reviewed_sha: "3e71a7ec972352774c3c5806a5e8303fc24a5c6e"
reviewer: "nan/deepseek-v4-flash"
date: "2026-09-21"
---
## Adversarial review

**Scope**: `CI-314-pr-agent-reviewer` — `git diff ebb544d2530f1504f366ebe2655579f045195e7d...HEAD` (13 commits, 36 files). The launcher-resolved base is the parent of `6301b38` (`ci: add PR-Agent as a second reviewer`, #315), so the range is the spec's own work (#315, #321, #323) **plus** five intervening merges that are not this spec: #320/#325 (hygiene guards, action SHA pins), #324 (release 1.17.3), #326/#327/#328 (dependabot). Graded below with that split stated rather than hidden.

**Sources**: `specs/CI-314-pr-agent-reviewer/{proposal,tasks,verification}.md`, `features.json`; `.github/workflows/pr-agent.yml`; `.pr_agent.toml`; `harness/review-attestation.json`; `.github/workflows/repo-hygiene.yml`; `scripts/{check-actions-pinned,check-lessons,pin-actions}.sh`; the pinned action source at `The-PR-Agent/pr-agent@f3b385ea2927247ddcff2fe252472380b9c8f5fc` (v0.45.0) fetched over the API; live Actions runs and PR comments via `gh`.

### Spec and task alignment

| AC | Proposal wording | Artifact state | Reality (evidence gathered in this session) |
|---|---|---|---|
| AC1 | first non-draft PR carries `## PR Reviewer Guide` from `github-actions[bot]`, guard green | `[ ]` — "green (needs a real PR + real inference)" (tasks.md:34-35) | **MET.** #315 merged `2026-09-01T02:49:11Z`; the very next non-draft PR (#316, `docs/agents-dead-paths`) ran `pr-agent` at `02:53:47Z` (run `33464237552`) with every step `success`, including `Fail if no review was published`; the guide was posted by `github-actions[bot]` at `02:54:49Z`. Reproduced on the currently pinned v0.45.0: run `35682751703` (#336, `2026-09-22T03:19:54Z`) — all seven steps `success` (including `Stamp run start` and `Fail if no review was published`), guide posted `03:23:31Z`. |
| AC2 | a Dependabot PR and a release-please PR produce no run | `[ ]` — "Observation pending merge" (tasks.md:36) / verification.md:13-15 | **MET in substance.** The workflow run record is created but the `review` job is `skipped`, so no PR-Agent step and no comment: release-please runs `34146702374`, `34081102575`; every dependabot run in `gh run list --workflow=pr-agent.yml --limit 100` is `skipped`. (Worth recording as "job skipped", not "no run" — the AC's own wording is looser than the mechanism.) |
| AC3 | pushing a fix re-triggers a review (`handle_push_trigger` + `push_commands`) | `[ ]` — "Observation pending merge" (tasks.md:37) | **MET.** #316: commit `540ca7a` pushed `03:04:30Z` → run `33464880724` at `03:04:36Z`, guard `success`. #325: push commits `08:05:55`, `08:18:00`, `08:31:12` → runs `34098785105`, `34099871549`, `34101022805`, each publishing a fresh review. |
| AC4 | coverage ≥ 90 % over the next 15 non-dependabot PRs | `[ ]` (correctly) | Not yet measurable — 7 non-dependabot PRs have run since merge, all reviewed. |
| AC5 | a PR CodeRabbit declines still gets read | `[ ]` | Directionally satisfied but not counted: on #337 CodeRabbit's comment is the `rate limited by coderabbit.ai` notice and the PR-Agent guide is present. The 15-PR count is the deliverable. |

Task boxes that are `[x]` are backed by the diff: the config/CI/registry files exist, and `yaml.safe_load` / `tomllib` / `json` parse them (reproduced below). No `[AGENT-DRAFT]`/`[AGENT-SUGGESTION]` tags remain (`grep` → none).

Both **Major** findings land in the contract set, which is why the verdict is FAIL rather than PASS WITH GAPS: `features.json` cannot reach `passing` as written, and `proposal.md`/`tasks.md` assert an unproven state that this repo's own PR history has already proven.

### Findings

| Severity | Reality | Area | Finding | Evidence | Test (named, or UNTESTED) | Fix location |
|---|---|---|---|---|---|---|
| Major | REAL | spec contract (`features.json`) | `CI-314-pr-agent-reviewer-f1`'s `verification` command can never exit 0, so the harness can never set `state: passing` for the feature that encodes AC1. It asserts step **position** (`s[0]=='PR-Agent'`), but #323 (same range) inserted `Stamp run start` and `Fail if the reviewer has no credential` before `PR-Agent`. | Ran the exact command from `features.json`: `['Stamp run start', 'Fail if the reviewer has no credential', 'PR-Agent', 'Fail if no review was published']` → `AssertionError`, `exit=1`. f2's command passes (`exit=0`). | UNTESTED — nothing runs the `verification` commands; `go test ./...` and `scripts/tests/smoke.bats` do not read `features.json`. | spec — `features.json` (**contract set**) → re-review. Fix is positional → membership, e.g. `'PR-Agent' in s`. |
| Major | REAL | spec truthfulness (`proposal.md`, `tasks.md`; registry prose) | The spec's central risk claim is false, and the acceptance evidence that disproves it is unrecorded. `proposal.md` §Risks: "…but has never **appeared in ts-bridge** because this workflow has not run here. The first green run is the verification; until then the registry entry is a declaration of intent, not of an actor." `harness/review-attestation.json`: "It has **NEVER been observed** in ts-bridge, because this workflow has not run here." `tasks.md`/`verification.md` still say the first run is a post-merge observation. | The workflow has run 76 times here, all since `2026-09-01T02:44:47Z` (`total_count` on the workflow's runs endpoint); the marker appears on #316 (`02:54:49Z`), #319, #321, #323, #325, #336, #337. The author's own triage comment on #316 ("Two items from the PR-Agent guide; one applied") pre-dates #321's edit of `proposal.md` (2026-09-06, status → `verifying`) that left the sentence in place. | UNTESTED — no test maps an AC to its observable evidence; the artifacts are prose. | spec — `proposal.md` + `tasks.md` (**contract set**) → re-review; `verification.md` and the registry `why` are outside the set and can be corrected in the same round. |
| Minor | REAL | repo hygiene | Review artifacts produced by `dotf spec review` are untracked and **not** gitignored, while `.pr_agent.toml`'s `[ignore]` already declares them machine artifacts (`specs/**/review-transcript.jsonl*`). A `git add -A` commits a 318,463-byte transcript. | `git status --porcelain` lists `specs/CI-314-pr-agent-reviewer/review-transcript.jsonl` (+`.stderr`, `review-request.json`) and the SEC-212 set; `grep -n review-transcript .gitignore` → no match; `ls -l` → 318,463 bytes. | UNTESTED | code — `.gitignore` (the producing tool is `dotf`, so the durable fix may belong in dotfiles; a line here stops the leak now). |
| Minor | REAL | attestation observability | On 4 of the 7 PRs reviewed since merge, PR-Agent could not update its persistent comment and published a **second** comment (`## Standalone PR Review`) instead — so a PR accumulates several marker-bearing bot comments, and the guard stays green from whichever one this run touched. It is correct today (the standalone body carries a real review), but the registry's single-marker model and the triage queue now read a page of near-duplicate guides. | #319 `2026-09-06T23:49:27Z`; #321 `23:55:42Z`; #323 `03:28:14Z` and `03:47:37Z`; #325 `08:20:55Z` and `08:35:34Z` (full bodies fetched; each carries `## PR Reviewer Guide 🔍` after the standalone header). Source: `pr_reviewer.py::_as_non_authoritative_review` strips only the identity marker, not the heading. | UNTESTED — the evidence is live comments, not a test. | spec (`verification.md`, free) — record it; upstream behaviour, no fix in this repo. |
| Minor | THEORETICAL | CI static checks | The new 257-line workflow and its 46-line shell guard are enforced by no CI job. `repo-hygiene.yml` (this range) added lesson numbering and SHA-pin guards; `actionlint`/`shellcheck` appear only inside a `pr-agent.yml` comment. Nothing keeps the flow-expressions and the guard's shell valid as they are edited. | `grep -rn "actionlint\|shellcheck" .github/workflows/ Makefile` → only the comment at `pr-agent.yml:116`. I ran both locally on the current tree: `actionlint -verbose .github/workflows/*.yml` → 0 errors on `pr-agent.yml` (1 pre-existing `SC2035` in `release.yml:47`, untouched by this diff); `shellcheck` is installed and was invoked by actionlint. | UNTESTED (the guard itself is the check, and it is not wired) | code — add a third step to `repo-hygiene.yml`. |
| Minor | THEORETICAL | config precedence across trigger paths | A manual `/review` (`issue_comment`) run does not load the repo's `.pr_agent.toml`, so `extra_instructions`, `[ignore] glob`, `require_tests_review` and `persistent_comment` fall back to upstream defaults; only settings duplicated as env (`CONFIG__*`, `github_action_config.*`) survive. The workflow's own "keep the two in step" note therefore holds only on the `pull_request` path. | Source at the pinned SHA: `github_action_runner.run_action` guards `apply_repo_settings(pr_url)` behind `event_payload["pull_request"]["html_url"]`, absent on `issue_comment`; `github_provider.get_repo_settings` reads `repo_obj.get_contents(".pr_agent.toml")` (default branch) — so the file is safe from a PR but simply not applied on that path. No `issue_comment` run has ever executed here (all `skipped`), so no live repro. | UNTESTED | code (workflow env) or spec note. |
| Minor | THEORETICAL | guard robustness | A returned comment with `body: null` aborts the `jq` pipeline before the diagnostic, turning a red-with-remedy into a red-with-a-`jq`-stack-error. Fails **closed**, so no false green. | Synthetic `gh` stub + the guard's `run:` block extracted from the YAML: scenario with `"body":null` → `jq: error (at <stdin>:1): null (null) and string ("## PR Revi...) cannot have their containment checked`, step exit 5. Same battery: fresh bot comment → exit 0; stale comment (previous run's guide) → exit 1; marker posted by a human login → exit 1; marker on a second API page → exit 0. | UNTESTED (no in-repo harness for `guard.sh`) | code — `(.body // "") \| contains($marker)`. |
| Minor | THEORETICAL | guard fallback | When the default branch declares no `github-actions` entry, the guard falls back to a **hardcoded** `marker="PR Reviewer Guide"` if the PR head declares one (the bootstrap case). Flagged by PR-Agent on #325 as an expanded attack surface. I could not construct a false green: the comment must still be authored by `github-actions[bot]` and carry an `updated_at` at or after this run's stamp, and the fallback text is PR-Agent's own heading, not attacker-chosen; the source strips only the identity marker so the heading is present on every real review. The path is also unreachable in steady state — the default branch has declared the entry since `2026-09-01`. | `pr_reviewer.py::_as_non_authoritative_review`; guard battery scenario `base-no-entry` → notice emitted, then exit 1 without a matching fresh bot comment. | UNTESTED | — (surface only; do not gate) |

Strengths that materially bound the risk above (stated because they answer specific findings, not as balance): the `Fail if the reviewer has no credential` step runs **before** the inference call with the remedy in the message, and the guard's start-stamp binding genuinely rejects a previous run's untouched guide (both reproduced). `.pr_agent.toml` is read from the default branch, so a PR cannot neuter its own review.

### Evaluator rubric

| Dimension | Grade (A-D) | Rationale (one line) |
|-----------|-------------|----------------------|
| Correctness | B | AC1-AC3 verified against live runs and the guard's negative paths are proven fail-closed; AC4/AC5 are inherently post-measurement, and one AC encodes into an unsatisfiable `features.json` command. |
| Verification | B | The measured tables are real and re-checkable (I re-verified the CodeRabbit marker table per PR: #293/#298/#301/#310 `## Walkthrough`, #294/#295/#296/#300/#311 `Review limited reached`, #297 `## Review skipped`), but the AC1-AC3 evidence was never recorded even though the author's own triage comments prove it was seen. |
| Scope | B | The spec's own commits are CI/config/spec/registry/lesson only — no Go change, no new project dependency, as the proposal claims; the reviewed range also carries four unrelated merges (dependabot, release 1.17.3, #320/#325 hygiene) from the launcher's base resolution, which dilutes the diff rather than reflecting creep. |
| Reliability | B | Fail-loud credential check, start-stamp binding, event-keyed concurrency, dependabot/fork exclusions; small gaps are the mute `jq` abort and the cross-path config asymmetry. |
| Maintainability | B | Dense WHY-comments in YAML/TOML, small shell functions; the model chain is stated twice (`[config].model` and `CONFIG__MODEL`) with only a comment enforcing "keep the two in step", and the head-registry fallback is unreachable in steady state. |
| Handoff-readiness | C | Lesson capture landed (lesson-030 plus the pointer fix in `docs/lessons.md`) and the next steps are clear, but the spec artifacts a reviewer must trust are stale on exactly the criterion this change exists to prove, and `features.json` cannot pass. |

### Verdict
FAIL

### Recommended next steps

Contract set (each edit invalidates this verdict by design — a fresh review round follows):

1. `features.json` — `CI-314-pr-agent-reviewer-f1`: assert membership, not position (`'PR-Agent' in s`), keeping the `Fail if no review was published` assertion. Treat this as the blocking edit: with it unfixed the feature's terminal state is unreachable.
2. `proposal.md` §Risks — replace the "Marker unproven here yet / never appeared in ts-bridge" bullet with the observed record (#316 `2026-09-01T02:54:49Z`, then #319/#321/#323/#325/#336/#337), or drop the claim and keep the evidence in `verification.md`; either way the surviving text must be true at archive time.
3. `tasks.md` — tick `[AC1]`, `[AC2]`, `[AC3]` with the run IDs and timestamps cited in the alignment table above (`33464237552`, `35682751703`; release-please `34146702374`/`34081102575` and the dependabot `skipped` runs; `33464880724` plus the #325 push series). Fix the "AC1-AC5 in `program proposal.md`" wording while in there — it names a file this repo does not have.

Outside the contract set (safe to apply in the same round, no re-review cost):

4. `verification.md` — record the AC1-AC3 evidence with run IDs/timestamps and the job-`skipped` nuance for AC2; keep AC4/AC5 open with the counting method named; note the repeated `## Standalone PR Review` comments as the observed publication shape.
5. `harness/review-attestation.json` — replace "NEVER been observed in ts-bridge" with the observed dates. The entry works (`dotf pr triage-queue` reports `#336 github-actions reviewed, never triaged`); only its provenance prose is stale.
6. Housekeeping to ticket or apply: `.gitignore` entries for `specs/**/review-transcript.jsonl*`, `specs/**/review-request.json`, `specs/**/*.stderr`; an `actionlint`+`shellcheck` step in `repo-hygiene.yml`; `(.body // "")` in the guard's `jq` expression.

Until 1-3 land and a new review passes, `dotf spec archive CI-314-pr-agent-reviewer` is **not** advisable — the archive pre-flight requires a fresh passing `review.md`, and this one is both FAIL and about to be invalidated by the contract edits it requests.
