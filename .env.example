# Required: Tailscale auth key
# Generate at: https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-xxxxx

# Required: Target address (Tailscale IP:PORT)
TS_TARGET=100.x.x.x:3389

# Optional (recommended): Stable alias for this client instance
# TS_INSTANCE_NAME=office-laptop

# Optional: Port range for deterministic local port selection in auto mode
# TS_PORT_RANGE=33389-34388

# Auto mode is ON by default for minimal friction.
# To disable auto mode explicitly, set:
# TS_AUTO_INSTANCE=false

# Alternative disable switch (takes precedence over TS_AUTO_INSTANCE):
# TS_MANUAL_MODE=true

# Advanced manual overrides (usually not needed in auto mode):
# TS_LOCAL_ADDR=127.0.0.1:33389
# TS_HOSTNAME=ts-bridge
# TS_STATE_DIR=./ts-state

# Optional: Connection timeout (default: 30s)
# TS_TIMEOUT=30s

# Optional: Max concurrent connections (default: 1000)
# TS_MAX_CONNECTIONS=1000

# Optional: Health/metrics endpoint (disabled by default)
# TS_HEALTH_ADDR=127.0.0.1:8080

# Optional: Enable verbose logging (default: false)
# TS_VERBOSE=true

# Optional: Log format - "text" or "json" (default: text)
# TS_LOG_FORMAT=text
