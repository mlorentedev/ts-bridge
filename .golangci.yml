run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - goconst
    - gocyclo
    - misspell
    - prealloc
    - unconvert
    - unparam

linters-settings:
  gocyclo:
    min-complexity: 15
  goconst:
    min-len: 3
    min-occurrences: 3
  gosec:
    excludes:
      - G117
  misspell:
    locale: US

issues:
  exclude-dirs:
    - site
  exclude-rules:
    - path: _test\.go
      linters:
        - gocyclo
        - goconst
        - unparam
    - linters:
        - unparam
      text: "acceptLoop.*result 0.*is always nil"
  max-issues-per-linter: 50
  max-same-issues: 5
